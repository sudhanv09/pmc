[package]
name = "pmc"
version = "0.1.0"
edition = "2024"

[dependencies]
walkdir = "2.5.0"
nanoid = "0.4.0"
chrono = "0.4"
regex = "1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["net", "macros", "rt-multi-thread", "io-util"] }

